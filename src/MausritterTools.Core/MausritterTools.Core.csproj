<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- The tests exercise internal helpers such as NameForge.Join directly, rather than
         widening the public API purely to make them reachable. -->
    <InternalsVisibleTo Include="MausritterTools.Core.Tests" />
  </ItemGroup>

</Project>
